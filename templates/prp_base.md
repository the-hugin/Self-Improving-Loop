# PRP: [Feature Name]

> **Product Requirements Prompt** — минимально необходимый пакет для реализации фичи с первого прохода.
> Создан: YYYY-MM-DD | Статус: [ ] Draft / [ ] Approved / [ ] In Progress / [x] Done

---

## 🎯 GOAL

**Что строим:**
<!-- ONE sentence -->

**Почему это нужно:**
<!-- Бизнес/продуктовая причина -->

**Что НЕ входит в scope:**
<!-- Явные ограничения — предотвращает scope creep -->
-
-

---

## ✅ SUCCESS CRITERIA

<!-- Чёткие, проверяемые критерии. Каждый — тест, который можно запустить -->
- [ ] [Конкретное поведение, которое можно проверить]
- [ ] [Ещё одно]
- [ ] Все существующие тесты зелёные
- [ ] Нет новых TypeScript ошибок
- [ ] Lint проходит

---

## 📚 CONTEXT

### Файлы для изучения (прочитай ПЕРЕД реализацией)
<!-- Конкретные файлы, которые агент должен прочитать -->
```
src/[relevant-file].ts      — [почему важен]
examples/[pattern].ts       — [паттерн для следования]
```

### Внешняя документация
<!-- Ссылки с объяснением, что именно там нужно -->
- `[URL]` — [что смотреть, какой раздел]

### Существующие паттерны
<!-- Как похожее уже реализовано в проекте -->
```typescript
// Пример существующего паттерна, которому надо следовать
```

### Critical Gotchas
<!-- КРИТИЧЕСКИЕ вещи, которые легко упустить -->
```
# CRITICAL: [Важное ограничение или особенность]
# CRITICAL: [Ещё одно]
# NOTE: [Менее критичное, но важное]
```

---

## 🏗️ IMPLEMENTATION BLUEPRINT

> Детальный план реализации. Агент следует ему строго.

### Step 1: [Название шага]
```
Файлы: [какие создать/изменить]
Действие: [что именно сделать]
```

### Step 2: [Название шага]
```
Файлы: [какие создать/изменить]
Действие: [что именно сделать]
```

### Step 3: [Название шага]
```
Файлы: [какие создать/изменить]
Действие: [что именно сделать]
```

### НЕ ДЕЛАТЬ:
- [Что агент не должен делать в рамках этого PRP]
- [Что намеренно оставлено за scope]

---

## 🔄 VALIDATION LOOP

> После каждого шага агент проверяет себя. Если проверка не проходит — исправляет ДО перехода к следующему шагу.

### Level 1: Синтаксис и стиль
```bash
npm run lint
npx tsc --noEmit
```
→ Должно пройти без ошибок

### Level 2: Юнит-тесты
```bash
npm run test -- --testPathPattern=[feature]
```
→ Все тесты зелёные

### Level 3: Интеграционная проверка
```bash
npm run dev
# Проверить: [конкретные сценарии для ручной проверки]
```
→ [Ожидаемое поведение]

### Level 4: Регрессия
```bash
npm run test
```
→ Все существующие тесты зелёные

---

## 📝 IMPLEMENTATION NOTES

> Заполняется агентом в процессе реализации

### Решения принятые в процессе:
-

### Отклонения от Blueprint (и почему):
-

### Что стоит учесть в будущем:
-
